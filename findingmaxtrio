import streamlit as st

def find_largest_number(a,b,c):
    return max(a,b,c)

def main():
    st.title("Find the Largest Number")

    # Input for three numbers
    a = st.number_input("Enter the first number:", value=0.0)
    b = st.number_input("Enter the second number:", value=0.0)
    c = st.number_input("Enter the third number:", value=0.0)

    # Find and display the largest number
    largest_number = find_largest_number(num1, num2, num3)
    st.write(f"The largest number is: {largest_number}")

if __name__ == "__main__":
    main()
